# 更新日志

<!-- 发版时，请同步更新 base/Version.py、version.txt，并在本文件顶部添加对应版本。 -->

## v0.7.4 - 2026-08-08

### 新增

- Ren'Py 增量翻译重构：按文件、语言和标签作用域区分编号对话块，源文变更时仅重译变化的语句，保留未变语句的已有译文
- 一键翻译新增「提取游戏内置隐藏文本并翻译」，可原生表达的界面文本写入 `translate strings` 块并生成 `renpybox_bytecode_strings.rpy`（默认开启，升级后自动生效）
- 运行时 HOOK 提取新增增量补全模式，只追加缺失项而不覆盖已有译文（默认开启，升级后自动生效）
- 大写缩写候选二次整体翻译验证，两次都未翻译才判定不译并记入项目级清单

### 优化

- 「应用翻译到游戏」改为后台线程执行，应用期间显示进度对话框，UI 不再阻塞
- 优化应用内更新下载进度显示，补充百分比、主题色和下载状态反馈
- 翻译回写改为原子写入并保留文件权限，写入目标限制在项目目录内
- 回写后重新解析 AST 校验译文完整性，拒绝静默产生部分写入结果
- 完整提取失败时自动恢复原始 TL 目录，增量提取可通过日志文件恢复中断状态
- 一键翻译可清除项目级判定不译记录，并可关闭会额外消耗额度的大写缩写二次确认

### 修复

- 修复重复对话跨作用域错误共用译文，以及源文变更后继续保留过期翻译的问题
- 修复自然语言被误判为结构文本而跳过翻译（如 `not ugly`、`Dance.`）导致占位符丢失
- 修复模型把提示词段落当作译文返回时被写进游戏翻译文件的问题，此类响应现在判为异常并重试
- 写入前后清理跨文件重复 `old` 条目，并删除空文件与空 `translate strings` 块，避免 Ren'Py 启动报错
- 修复清理空翻译文件时误删用户纯注释 `.rpy` 及同名 `.rpyc` 的问题
- 修复工具位置注释残留导致空翻译文件无法清理的问题
- 收窄无需翻译的缩写范围，避免短信、会员、编号等界面文本保留英文
- 原子写入恢复平台默认行尾，避免 Windows 翻译目录产生整文件差异
- 写回失败时保留可恢复备份，兜底注入仍会执行；手动导出与缓存重新注入失败时会给出错误提示

## v0.7.3 - 2026-08-03

### 新增

- 接口管理页按本地模型、传统机翻、在线大模型、自定义接口四组展示，顶部显示当前激活接口，空分组自动隐藏

### 优化

- DeepSeek 默认模型更新为 `deepseek-v4-flash`（官方已于 2026-07-24 停用 `deepseek-chat` 别名），启动时自动迁移官方端点的旧模型名，自定义中转接口保持原值
- DeepSeek 思考档位补全「低」档，接口测试失败时会显示接口返回的具体错误消息

### 修复

- 修复启用结构化输出后，接口测试、术语抽取、AI 分析、单行翻译和术语库翻译被错误套用 JSON 对象约束的问题，DeepSeek 上表现为报错「Prompt must contain the word 'json'」
- 输出格式约束改为按每次请求的实际协议决定，不再复用全局翻译协议设置
- 接口参数错误（HTTP 400）不再无意义地重试三次，直接返回并记录原因
- 移除小于 100% 的界面缩放档位，修复控件错位

## v0.7.2 - 2026-07-31

### 新增

- 应用设置新增「关于与更新」，可查看当前版本、手动检查更新、查看发布说明并带进度条下载，支持取消
- 新增应用内更新日志查看，升级后首次启动会展示当前版本的变更
- Ren'Py 工具箱搜索支持别名与关键词（如 rpa、apk、反编译），无匹配时显示空状态提示

### 优化

- 重构 Ren'Py 工具箱入口与导航，按工作流分组工具并统一图标与返回行为
- 工具箱卡片支持键盘操作与按压反馈，未就绪的工具会标注「需先选择游戏目录」
- 卡片宽度随窗口自适应，各分组右边缘对齐

### 修复

- 修复下载更新的临时文件路径在非安装目录启动时不一致，导致更新包找不到的问题
- 下载完成后校验文件大小与 sha256，校验失败会删除临时文件并回滚状态
- 安装更新前检查是否有任务在运行，避免静默中断翻译
- 修复更新日志中版本号与远端发布说明重复展示的问题

## v0.7.1 - 2026-07-26

### 修复

- 修复 Alternative Saves SDK 的旧 RAPT 项目缺失 `res/xml/backup.xml` 时 Android 资源链接失败的问题

## v0.7.0 - 2026-07-26

### 新增

- 新增项目级世界观、角色卡、术语和禁翻项管理，修复旧配置迁移可能覆盖或丢失项目数据的问题
- 新增初译质量报告、AI 校对和润色，支持停止任务、筛选问题条目及批量确认重试译文

### 优化

- 重构翻译流程，增加翻译前检查、任务快照和更清晰的运行状态，停止、暂停及续译更稳定
- 重构提示词设置，基础模式、写作风格和输出协议可独立选择，并支持查看、复制当前提示词
- 优化一键翻译和 Ren'Py 工具箱，支持从当前项目跳转 RPA 解包、自动带入路径，并按常用流程整理工具入口

### 修复

- 修复 Token 估算崩溃、累计时间跳动、任务状态异常及批量字体替换闪退等问题

