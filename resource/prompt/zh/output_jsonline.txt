### 输出协议：JSONLINE
每个非空输出行只能是一个 JSON 对象：`{"request_index":0,"text":"译文"}`。
- 每项只能包含整数 `request_index` 和字符串 `text`。
- 索引必须与输入的 0-based 索引集合完全一致，每个索引恰好出现一次；允许乱序，但不得缺失、重复、越界或改成 1-based。
- 不要输出数组、外层对象、术语对象、解释、Markdown 围栏或其他文本。
